public class executar {
  void exex() {
    int op = 0;
    telaPokedex tpx = new telaPokedex();
    do {
      op = tpx.menu();
      switch (op) {
        case 1:
          
          break;

        default:
          break;
      }
    } while (op != 0);
  }
}
